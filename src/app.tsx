import { Button, Composite, TextView, WebView, ui } from 'tabris';



ui.contentView.append(
    <composite>
        <webView  left={0} right={0} top={0} bottom={0} url='http://www.skynetsoftware.ga/'/>
    </composite>
);

function change(){

}
